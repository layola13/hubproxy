# Deno Test Coverage Matrix

Updated: 2026-06-11

This matrix is the working audit plan for porting the Deno implementation to SA without discovering one missing behavior at a time. It is organized by Deno test behavior, not by implementation file.

Current counts:
- Deno source tests audited here: 67 `Deno.test(...)` cases from `src/proxy_test.ts`, `src/handlers_test.ts`, `src/env_test.ts`, `src/state_test.ts`, `src/responses_mock_test.ts`, and `src/real_upstream_test.ts`.
- SA focused tests currently available in this worktree: 19 `.sa` files. Default unit evidence is native `sa test`; isolated `.sh` runtime contracts are historical/manual runtime probes only and should not be used as the unit-test vehicle for new parity coverage.

## Proxy / OpenAI Compatibility

| Deno behavior group | SA coverage | Status |
| --- | --- | --- |
| Auth forwarding, client auth header stripping, base URL routing, model passthrough | `http_dispatch_contract_test.sa` covers the shared upstream auth-header builder used by models/chat/responses proxy paths, proving upstream `authorization` is derived from the configured API key as `Bearer <key>` rather than caller auth. It also covers the native upstream forward-header plan helper, proving the SA upstream whitelist emits configured `authorization`, configured `x-api-key`, and JSON `content-type` while documenting Deno-sensitive dropped names (`authorization`, `x-api-key`, `api-key`, `content-length`, `connection`, `host`) and the empty-api-key auth omission branch. It also covers the production upstream URL helper used by models/chat/responses/fallback paths, proving `CHAT_BASE_URL` routes to `/models` and `/chat/completions`, while `RESPONSES_BASE_URL` routes to `/responses` and responses chat fallback routes to `/chat/completions`. The same native gate covers the production local-auth matcher used by `check_auth`: Deno-style `Authorization: Bearer <token>` is accepted, raw or embedded `Bearer` authorization values are rejected, bad Bearer tokens are rejected, and `x-api-key` keeps exact raw-token matching, plus the production public-model route helper where only `GET /v1/models` bypasses auth. `test_models_runtime_isolated.sh` builds a temporary SA binary, starts HubProxy with generated `.env` and a mock upstream on temporary ports, verifies live `/v1/models` dispatch to the configured `CHAT_BASE_URL`, proves upstream socket capture sees configured `authorization: Bearer <OPENAI_API_KEY>` and configured `x-api-key`, proves caller `authorization`/`x-api-key`/`api-key` material is not forwarded, and now verifies common Deno non-sensitive passthrough headers (`openai-organization`, `openai-project`, `x-request-id`, `accept-language`) reach the upstream socket. `test_model_list_rpc_runtime_isolated.sh` verifies authenticated live `/rpc` `model/list` reuses the upstream `/v1/models` proxy path, returns the upstream raw body/status/content-type without a JSON-RPC wrapper, and strips caller auth while forwarding configured upstream auth. `test_responses_large_runtime_isolated.sh` adds direct live `/v1/responses` socket capture for both JSON and SSE requests, verifying configured upstream auth and caller auth stripping on the native Responses path. `test_chat_responses_schema_runtime_isolated.sh` adds direct live `/v1/chat/completions` and `/v1/responses` dispatch capture on temporary ports, verifying both paths use configured upstream `authorization`/`x-api-key`, strip caller `authorization`/`x-api-key`/`api-key` material, and passthrough the same common non-sensitive headers on both direct proxy paths. `test_http_contract.sa` keeps broad smoke coverage; deleted shell tests `test_upstream_auth_headers.sh`, `test_chat_tools_capture.sh`, `test_responses_tools_capture.sh`, `test_empty_body_reject.sh` are historical migration references | Covered by current native default gate for configured upstream auth construction, sensitive client/hop-by-hop header stripping plan, upstream route/base suffix selection, local Bearer/x-api-key auth semantics, and public-model route gating, plus isolated runtime coverage for live `/v1/models` base dispatch/model passthrough/socket-level auth stripping, live `/rpc model/list` upstream model passthrough, direct live chat dispatch, direct live `/v1/responses` configured-auth/caller-auth-strip capture, and common non-sensitive header passthrough. Full arbitrary non-sensitive header enumeration remains separate SA HTTP facade work |
| Empty JSON body rejection before upstream call | `http_dispatch_contract_test.sa` covers the production `body_is_blank` helper used before `/v1/chat/completions` and `/v1/responses` upstream calls, including Deno `trim()`-style empty/ASCII-whitespace bodies and non-empty JSON bodies; `test_empty_body_runtime_isolated.sh` builds a temporary SA binary, starts HubProxy with generated `.env` plus a mock upstream on temporary ports, sends empty and ASCII-whitespace bodies to both `/v1/chat/completions` and `/v1/responses`, verifies local HTTP 400 rejection, and proves the mock upstream receives zero POST requests; deleted `test_empty_body_reject.sh` is a historical migration reference | Covered by current native default gate for local blank-body detection plus isolated live no-upstream-call evidence for empty and whitespace chat/Responses proxy requests |
| `/v1/models` passthrough and public models with protected `/rpc` | `http_dispatch_contract_test.sa` covers configured upstream auth-header construction shared by `/v1/models`, the native upstream header-plan helper for configured auth plus stripped sensitive client/hop-by-hop header names, the production upstream URL helper for the `/models` target, the production public-model route helper (`GET /v1/models` only), and the production local-auth matcher used by protected `/rpc` for strict Bearer authorization and exact `x-api-key` matching; `test_models_runtime_isolated.sh` covers the live public `/v1/models` path on temporary ports with a mock upstream, verifies the upstream model body is passed through, captures configured upstream auth headers without caller secret leakage, verifies common non-sensitive passthrough headers reach upstream, and verifies unauthenticated `/rpc` remains HTTP 401 in the same isolated process; `test_model_list_rpc_runtime_isolated.sh` covers authenticated live `/rpc` `model/list` passthrough to the same upstream `/v1/models` path, including raw upstream body/status/content-type, configured upstream auth, and caller auth stripping without a JSON-RPC envelope; `test_http_contract.sa` keeps broad public-model/protected-RPC shape; deleted `test_upstream_auth_headers.sh` is a historical migration reference | Covered for broad shape, native upstream auth construction, native sensitive-header strip plan, native `/models` target selection, local public-model route gating, local auth matching semantics, isolated live public and RPC model passthrough, socket-level configured-auth capture, common non-sensitive header passthrough, and protected `/rpc` boundary. Full arbitrary non-sensitive header enumeration remains separate SA HTTP facade work |
| Responses base URL missing or unavailable falls back to chat | `responses_chat_fallback_request_test.sa` request-construction coverage; `http_dispatch_contract_test.sa` covers the production upstream URL helper proving native Responses targets `/responses` while responses chat fallback targets `/chat/completions`; `test_responses_fallback_runtime_isolated.sh` builds a temporary SA binary, starts HubProxy with generated `.env` that intentionally omits `RESPONSES_BASE_URL`, uses a mock chat upstream on a temporary port, posts authenticated `/v1/responses`, and verifies the live request is sent to `/v1/chat/completions` with configured upstream auth plus a normalized Responses JSON response body; `test_responses_unavailable_fallback_runtime_isolated.sh` configures both `RESPONSES_BASE_URL` and `CHAT_BASE_URL`, captures the live first `/v1/responses` request returning 404, then verifies SA retries through `/v1/chat/completions` with configured upstream auth and normalized Responses JSON output; deleted runtime shell tests `test_responses_missing_base_capture.sh`, `test_responses_fallback_capture.sh`, `test_responses_fallback_stream_events.sh` are historical migration references | Covered by current native/plugin gate for request construction and target suffix selection, plus isolated runtime evidence for live missing-`RESPONSES_BASE_URL` fallback and live configured-Responses 404 fallback. Other fallback-trigger status codes can get focused isolated checks if needed, but the primary unavailable upstream path now has live evidence |
| Chat fallback strips responses-only fields and preserves user input/model | `responses_chat_fallback_request_test.sa`; `test_responses_fallback_runtime_isolated.sh` captures the live missing-base fallback upstream body and verifies `model`, `stream:false`, instructions/developer/user text, and a flattened namespace tool are preserved while `store`, `prompt_cache_key`, `include`, `reasoning`, and raw `input` do not leak into the chat request; `test_responses_unavailable_fallback_runtime_isolated.sh` captures the live configured-Responses 404 retry path, proving the native Responses request first preserves Responses-only fields for `/v1/responses` and the subsequent chat fallback strips them while preserving `model`, `stream:false`, instructions, and user text | Covered for request normalization plus isolated live body capture on both missing-base fallback and configured-Responses 404 fallback |
| Native Responses preserves responses-only fields; Gemini strips incompatible fields | `responses_chat_fallback_request_test.sa` now covers the production `build_responses_proxy_body` path: non-Gemini native Responses keeps `store`, `prompt_cache_key`, `include`, `reasoning`, and `input`; Gemini mode drops only `store` and `prompt_cache_key` while keeping `include`, `reasoning`, and `input`. `test_chat_responses_schema_runtime_isolated.sh` captures a live non-Gemini `/v1/responses` upstream request and verifies `store`, `prompt_cache_key`, `include`, `reasoning`, and `input` survive the direct Responses proxy path. Deleted runtime shell tests `test_responses_gemini_capture.sh` and `test_responses_gemini_tool_history_no_fallback.sh` are historical migration references | Covered by current native default gate for request-body construction plus isolated live proxy capture for non-Gemini direct Responses; Gemini stripping remains native-covered and can get a separate isolated socket capture if needed |
| Tool schema normalization for chat and responses, including namespace tools and non-function filtering | `responses_chat_fallback_request_test.sa` covers namespace/MCP request normalization and the SA production `build_responses_proxy_body` namespace expansion path for Code Index/Mimir tools; `test_responses_fallback_runtime_isolated.sh` adds one live missing-base fallback capture proving namespace tools reach the chat upstream as flattened MCP function tools; `test_chat_responses_schema_runtime_isolated.sh` captures direct live `/v1/chat/completions` and `/v1/responses` upstream bodies, proving chat tools are emitted as nested OpenAI function schemas, Responses tools are emitted as flat function schemas, namespace tools are expanded to `mcp__code_index__search`, and top-level plus namespace-child non-function tools are filtered out. Deleted shell tests `test_chat_tools_capture.sh`, `test_responses_tools_capture.sh`, `test_responses_fallback_capture.sh` are historical migration references | Covered by current native/plugin gate plus isolated live fallback, direct chat, and direct Responses schema capture, including namespace expansion and non-function filtering |
| Function-call history fallback and tool message name preservation | `responses_chat_fallback_request_test.sa`; SCI/plugin focused fixture for `sa_deno_responses_chat_fallback_request`; `test_responses_fallback_runtime_isolated.sh` now includes live missing-base fallback socket capture for Responses `function_call` plus `function_call_output` history, proving the upstream chat request contains an assistant `tool_calls` message with the original `call_id`/tool name/arguments and a following `role:"tool"` message preserving `tool_call_id`, `name`, and output content. Deleted `test_responses_fallback_tool_history.sh` is a historical migration reference | Covered for current native/plugin request-construction path plus isolated live proxy history capture through the missing-base fallback route |
| Chat fallback tool-call stream normalization, split chunks, `read` -> `exec_command`, `.env*` redaction, shell quoting | `responses_chat_fallback_request_test.sa` covers the installed deno plugin `sa_deno_plugin_chat_sse_to_responses` path for Deno stream tool-call normalization: `read` becomes `exec_command` with `cat '/tmp/demo.txt'`, direct `exec_command.command` becomes `cmd`, split tool-call chunks merge into one `call_pwd` with `{"cmd":"pwd"}`, `.env` reads become a redacted `sed -E` command instead of `cat`, and paths containing single quotes are shell-quoted. `test_responses_fallback_runtime_isolated.sh` now adds live missing-base `/v1/responses` fallback stream evidence on temporary ports: the mock chat upstream emits SSE tool-call chunks for read, exec, env read, single-quote paths, and split arguments, and the client-visible Responses SSE is asserted for preserved `call_id`s, normalized `cmd` arguments, env redaction, shell quoting, no raw `read` tool name, and merged split chunks. Deleted shell tests `test_responses_fallback_stream_tool_call_normalize.sh`, `test_responses_fallback_stream_tool_call_split.sh`, `test_responses_fallback_stream_tool_call_read_env_redact.sh`, `test_responses_fallback_stream_tool_call_read_shell_quote.sh` are historical migration references | Covered for current native/plugin normalization fixtures plus isolated live proxy streaming behavior through the missing-base fallback route |
| Chat fallback namespaced tool-call de-flattening | `responses_chat_fallback_request_test.sa` formatted/flat MCP namespace coverage; `test_responses_fallback_runtime_isolated.sh` now adds live missing-base fallback stream evidence where the original Responses request declares the `mcp__demo_server__` namespace and the mock chat upstream emits a `demo-server.rebuild` tool call; the client-visible Responses SSE is asserted for `namespace:"mcp__demo_server__"`, short `name:"rebuild"`, preserved `call_demo`, and no raw dotted `demo-server.rebuild` leakage. Deleted `test_responses_fallback_stream_tool_call_namespace.sh` is a historical migration reference | Covered for native/plugin normalization plus isolated live proxy stream de-flattening behavior through the missing-base fallback route |
| Native Responses SSE MCP de-flatten and server denormalize for `response.output_item.done` and `response.output_item.added` | `responses_chat_fallback_request_test.sa`; plugin `zig test src/hubproxy_compat.zig`; deleted `test_responses_client_mcp_denormalize.sh` and `test_responses_client_mcp_added_normalize.sh` are historical migration references | Covered by current native/plugin gate |
| Request-side MCP server alias normalize | `responses_chat_fallback_request_test.sa`; plugin `zig test src/hubproxy_compat.zig`; deleted `test_responses_mcp_server_normalize.sh` is a historical migration reference | Covered by current native/plugin gate for mapped, double-wrapped, and generic aliases |
| Long Responses request bodies and large normalized SSE bodies do not corrupt SA heap or disconnect Codex streams | `test_responses_large_runtime_isolated.sh` builds a temporary SA binary, starts HubProxy and a mock Responses upstream on temporary ports, sends a non-stream `/v1/responses` request with a 90KB input tail marker and verifies the upstream body is not truncated plus the JSON response returns intact, then sends a stream request and verifies a 90KB SSE response body reaches the client with the tail marker and `response.output_text.delta` event present. Deleted shell tests `test_responses_long_input_request_no_crash.sh`, `test_responses_native_large_sse_no_crash.sh`, plus prior live `codex exec` long prompt smoke are historical references | Covered by isolated runtime evidence for direct native `/v1/responses` long request body forwarding and large SSE response delivery without truncation/disconnect |

## Reasoning / Thinking / Progress

| Deno behavior group | SA coverage | Status |
| --- | --- | --- |
| Chat JSON fallback content/usage/tool calls/thought tags/`reasoning_content` -> Responses JSON | `responses_chat_fallback_request_test.sa` now covers the installed deno plugin `sa_deno_plugin_chat_json_to_responses` path for Deno chat JSON fallback: `reasoning_content`, `<thought>` extraction, visible assistant text, usage normalization, and tool-call preservation are asserted through the default SA native gate; deleted shell tests `test_responses_fallback_json_tool_call.sh`, `test_responses_fallback_json_tool_only.sh`, `test_responses_fallback_json_thought_tag.sh`, `test_responses_fallback_json_reasoning_content.sh` are historical migration references | Covered by current native/plugin default gate |
| Chat stream `thinking`/`reasoning_content` and split `<thought>` tags -> Responses reasoning events | `responses_chat_fallback_request_test.sa` now covers the installed deno plugin `sa_deno_plugin_chat_sse_to_responses` path for Deno chat SSE fallback: `reasoning_content`, `thinking`, split `<thought>` content, reasoning completion ordering, and visible answer deltas are asserted through the default SA native gate; deleted shell tests `test_responses_fallback_stream_reasoning_content.sh`, `test_responses_fallback_stream_thought_tag.sh` are historical migration references | Covered by current native/plugin default gate |
| Chat fallback Responses SSE event ordering keeps Codex active message item across multiple text deltas | `responses_chat_fallback_request_test.sa`; SCI focused fixture for `sa_deno_chat_sse_to_responses` | Covered through SA std `sa_deno_chat_sse_to_responses` |
| Native Responses stream/JSON thinking normalization | `responses_chat_fallback_request_test.sa` covers the installed deno plugin native Responses normalizers for both Deno thinking paths: SSE `response.output_item.done` with `type:"thinking"` becomes a `type:"reasoning"` item preserving `native stream think`, and JSON output/message `thinking` or `reasoning_content` becomes reasoning items preserving `native json think` and `message json think` while keeping the assistant output text. `test_chat_responses_schema_runtime_isolated.sh` now adds direct live `/v1/responses` JSON capture proving upstream `thinking` output and message `reasoning_content` are returned to the client as `reasoning` without raw `thinking`/`reasoning_content` leakage, while preserving assistant output text. `test_responses_large_runtime_isolated.sh` now adds direct live `/v1/responses` SSE capture proving an upstream `response.output_item.done` `thinking` item is normalized to `reasoning` while still delivering the large output-text SSE body and tail marker. Deleted shell tests `test_responses_native_thinking_stream.sh`, `test_responses_native_thinking_json.sh` are historical migration references | Covered by current native/plugin normalization fixtures plus isolated live JSON and SSE proxy capture for direct native Responses thinking normalization |
| Progress-only continuation in goal/code/default contexts | `responses_chat_fallback_request_test.sa` now covers goal, code, and `# Collaboration Mode: Default` request contexts with `exec_command`, Deno progress phrases, and the native continuation SSE builder; deleted shell tests remain historical migration references | Covered by current native default gate for helper/request-context behavior; live proxy injection remains isolated runtime evidence |
| No continuation in ordinary mode, proposed plan output, normal final answer, or when `exec_command` is unavailable | `responses_chat_fallback_request_test.sa` now covers plan-context rejection, missing `exec_command`, proposed-plan output, final/summary output, and normal final text; deleted shell tests remain historical migration references | Covered by current native default gate for helper/request-context behavior; live proxy injection remains isolated runtime evidence |

## Handlers / JSON-RPC / State

| Deno behavior group | SA coverage | Status |
| --- | --- | --- |
| Thread start/resume/fork/read/list/turns lifecycle, string thread ids, metadata `gitInfo`, fork inheritance, unarchive empty-turn response, inject before reset, dynamic realtime/attestation ids | `thread_rpc_contract_test.sa` now covers rollback semantics, active-list archive/unarchive behavior, `thread/started` notification thread-object shape for caller-provided string `id`/`sessionId`, lifecycle notification frames for `thread/status/changed`, `thread/name/updated`, `thread/archived`, `thread/closed`, `thread/unarchived`, and `thread/goal/cleared` with external string `threadId`, `thread/resume` envelope writer behavior including state-owned model/provider/cwd/default approval fields and dynamic stored turn serialization, `thread/inject_items` parser/state/response-core behavior plus injected item lifecycle notifications, and thread object serialization for `forkedFromId`, `gitInfo`, `name`, provider, ephemeral flag, and empty turns; `test_thread_rpc_transport_runtime_isolated.sh` remains historical isolated runtime evidence for live `/rpc` thread envelopes; `mcp_rpc_contract_test.sa` covers realtime start result writer UUID v4-shaped dynamic turn ids/timestamps, realtime appendAudio/appendText/stop result writers, realtime started event params plus notification queue fixed-placeholder rejection, and attestation `attest_<uuid-v4>` token writing; `unit_tests.sa` covers state-level string thread ids and fork metadata copy; `test_http_contract.sa` keeps broad compile/test coverage; `model_list_contract_test.sa` still contains ignored runtime-client contracts that compile with `--compile-only --include-ignored`; deleted shell tests such as `test_thread_start_fork_param_overrides.sh`, `test_thread_resume_envelope.sh`, `test_thread_rollback_numeric_num_turns.sh`, `test_thread_string_id_lifecycle.sh`, `test_inject_items_lifecycle.sh`, `test_realtime_uuid.sh`, `test_attestation_uuid.sh`, `test_turn_*`, and `test_thread_*` are historical migration references | Covered by current native default gate for parser/state/writer/notification semantics, including detailed thread lifecycle event frames, native realtime start/append/stop response writer shape, and dynamic realtime/attestation ids. Live `/rpc` and `/events` HTTP transport snapshots remain non-unit runtime evidence, not the default SA gate |
| Standard JSON-RPC whitespace from normal JSON serializers preserves `id`, string ids, params, goal budget, and thread start envelope fields | `rpc_parse_contract_test.sa` verifies numeric and string JSON-RPC id token preservation, quoted string-id stripping for lookup, whitespace-tolerant `threadId`/`model`/`modelProvider`/`cwd`/`ephemeral`/`tokenBudget` parsing, and thread-start envelope field emission through production parser/writer helpers. `test_rpc_transport_runtime_isolated.sh` builds a temporary SA binary, starts HubProxy from a temporary cwd on a random localhost port, and verifies live `/rpc` JSON-RPC response envelopes preserve string ids and numeric ids for deterministic success methods plus the unknown-method error path. `model_list_contract_test.sa` still keeps the older ignored runtime-client HTTP contract as isolated evidence | Covered by current native default gate for parser and envelope construction plus isolated live `/rpc` envelope transport for deterministic success/error responses; thread/turn/goal-specific live response snapshots remain separate runtime evidence |
| Escaped JSON-RPC `params` strings preserve quotes and backslashes when echoed in thin responses | `model_list_contract_test.sa` ignored runtime-client contract; SCI `std_smoke_core.zig` JSON-RPC params literal fixture; `test_rpc_transport_runtime_isolated.sh` now writes a standard `json.dump` request for live `/rpc` `environment/add` with `name: env "quoted"` and `path: /tmp/a\\b`, then verifies the JSON-RPC response decodes and echoes both strings intact | Covered for parser/std behavior plus isolated live `/rpc` transport echo of escaped quote/backslash params; not part of the default native gate |
| Turn context resolution from `thread-id` / `turn-id`, stale turn ignored, body-inferred modes | `responses_chat_fallback_request_test.sa` covers string thread-id state lookup, numeric thread-id fallback, current-turn state mode precedence, stale turn-id fallback to body, and body-inferred goal/code/default modes through `response_request_context_mode_from_ids`; deleted shell tests `test_responses_turn_context_goal_continuation.sh` and `test_responses_turn_context_deno_parity.sh` are historical migration references | Covered by current native default gate for request-context mode resolution; live HTTP header extraction remains covered by production wrapper/build evidence |
| `turn/start` RPC collaboration mode source is restricted to `params.collaborationMode.mode`, not Responses-only aliases, with Deno-style ASCII trim/lowercase and arbitrary mode round-trip | `turn_rpc_contract_test.sa` covers explicit `params.collaborationMode.mode`, trim/lowercase for known modes, arbitrary mode text storage/round-trip, empty mode clearing, and rejection of `collaboration_mode.kind` / top-level `collaborationModeKind`; `test_thread_rpc_transport_runtime_isolated.sh` adds live `/rpc` evidence that `turn/start` accepts `params.collaborationMode.mode`, normalizes `" Goal\t"` to `collaborationModeKind:"goal"`, stores input message items, and returns the turn object inside a JSON-RPC envelope; deleted `test_turn_start_collaboration_mode_rpc_parity.sh` is a historical migration reference | Covered by current native default gate for parser/state behavior plus isolated live `/rpc` turn/start response shape for the Deno explicit-mode path |
| `turn/steer` requires `expectedTurnId` to identify an existing turn; missing, malformed, or unknown ids return Deno-style `turn not found` instead of steering the first SA turn. Steer updates `updatedAt` even for empty or missing input, while initial `turn/start` input keeps newTurn timestamps equal. | `turn_rpc_contract_test.sa` covers append semantics, empty-turn first steer, empty steer timestamp advancement, missing-input handler fallback through the shared empty-array state path, and unknown `expectedTurnId` not mutating the first turn; deleted `test_turn_steer_items.sh` is a historical migration reference | Covered by current native gate for state/update semantics; HTTP response envelope still relies on existing dispatcher/build coverage |
| `thread/rollback` returns the thread with remaining turns, accepts numeric `numTurns`, keeps all turns for `0`, and clears all turns when the requested rollback count exceeds the active turn count | `thread_rpc_contract_test.sa` covers numeric and string `numTurns` parsing, missing `numTurns` defaulting to 1, removing the last N turns, zero-count preservation, over-large rollback clearing active turns, and missing-thread not-found behavior; `test_thread_rpc_transport_runtime_isolated.sh` verifies the live `/rpc` `thread/rollback` envelope removes the previously started turn for a caller-provided string `threadId` with numeric `numTurns:1` and returns the thread object with empty `turns`; deleted `test_thread_rollback_numeric_num_turns.sh` is a historical migration reference | Covered by current native gate for parser/state behavior plus isolated live `/rpc` rollback response envelope for numeric `numTurns` |
| Goal lifecycle, current timestamps, repeated set preserving `createdAt` while advancing `updatedAt`, turn timestamps stable across `turn/start` -> `thread/read`, interrupted turns preserving start timestamps while setting `completedAt`, and goal/item event timestamp/thread-id parity | `test_state.sa` state-level coverage; `goal_rpc_contract_test.sa` now verifies repeated goal set preserves `createdAt`, advances `updatedAt`, updates objective/status/budget like Deno, and emits `thread/goal/updated` frames with external `threadId` at both params and nested goal levels while omitting Codex-only `turnId`; `turn_rpc_contract_test.sa` now verifies interrupted turns preserve `createdAt`, set `completedAt`, and keep `completedAt == updatedAt`; `item_rpc_contract_test.sa` verifies `item/started` and `item/completed` notification frames include Deno-style `startedAtMs` and `completedAtMs` timestamp fields through the shared notification queue; `test_thread_goal_timestamp_runtime_isolated.sh` builds a temporary SA binary, starts HubProxy from a generated temporary `.env` on a random localhost port, opens authenticated `/events`, and verifies live `/rpc` `thread/goal/set` repeated updates preserve `createdAt` while advancing `updatedAt`, `thread/goal/get` returns the same goal object, `turn/start` timestamps remain stable through `thread/read`, `turn/interrupt` preserves turn `createdAt` while setting `completedAt == updatedAt`, and live `thread/goal/updated` plus `turn/completed` SSE frames carry the same timestamps and external string `threadId`. Deleted shell tests `test_goal_timestamp_current.sh`, `test_goal_update_timestamp_parity.sh`, `test_thread_timestamp_current.sh`, `test_turn_timestamp_current.sh`, `test_item_event_timestamp_current.sh`, `test_turn_goal_event_thread_id_parity.sh` are historical migration references | Covered by current native default gate for state/writer/notification semantics plus isolated live `/rpc` and `/events` timestamp transport evidence |
| Goal status covers the full Codex/Deno enum (`active`, `paused`, `blocked`, `usageLimited`, `budgetLimited`, `complete`) and preserves unknown runtime status strings like the current Deno handler; `tokenBudget` matches Deno `typeof number` behavior: `null`/missing/string become JSON null and signed numeric values round-trip | `goal_rpc_contract_test.sa` covers goal status parsing, unknown status state preservation, missing-thread string key preservation, string/null/missing budget as null, and signed numeric budgets; `test_thread_goal_timestamp_runtime_isolated.sh` adds live `/rpc` evidence for numeric `tokenBudget`, custom status preservation, `budgetLimited` with `tokenBudget:null`, and `usageLimited` with string `tokenBudget` returning JSON null in the response envelope; deleted `test_goal_status_budget_null_parity.sh` is a historical migration reference | Covered by current native default gate for parser/state/writer behavior plus isolated live `/rpc` HTTP envelope evidence for numeric, null, and string budget cases |
| `memory/reset` clears runtime state and emits wildcard close event | `thread_rpc_contract_test.sa` covers clearing thread, turn, goal, process, and watch state plus wildcard `thread/closed` notification containing `"threadId":"*"`; `test_state.sa` also has state-level reset coverage; `test_thread_rpc_transport_runtime_isolated.sh` now covers the live `/rpc` response envelope for `memory/reset`, verifying `reset:true`, external `threadId:"live-thread"`, and an empty `thread/list` after reset in the same isolated process; deleted `test_memory_reset_clears_state.sh` is a historical migration reference | Covered by current native gate for state/event semantics plus isolated live `/rpc` reset response and post-reset empty-list evidence |
| Events bridge for thread, turn, item, fs, process, realtime, MCP, warnings, including full turn objects in start/completed notifications | `unit_tests.sa` warning notification coverage, `thread_rpc_contract_test.sa` verifies Deno-shaped `thread/started` frames with full thread objects and external string `id`/`sessionId`, plus queue-backed `thread/status/changed`, `thread/name/updated`, `thread/archived`, `thread/closed`, `thread/unarchived`, and `thread/goal/cleared` frames with external `threadId` and Deno payload fields, `goal_rpc_contract_test.sa` verifies Deno-shaped `thread/goal/updated` frames with external thread keys and no Codex-only `turnId`, `turn_rpc_contract_test.sa` verifies Deno-style external thread key semantics plus full `turn` objects for `turn/started` and `turn/completed`, `item_rpc_contract_test.sa` verifies Deno item event order, `item/started` `startedAtMs`, `item/completed` `completedAtMs`, and sub-event families (`item/agentMessage/delta`, reasoning summary/text deltas, `item/plan/delta`, `item/fileChange/patchUpdated`, `item/commandExecution/outputDelta`, `item/mcpToolCall/progress`, `rawResponseItem/completed`, and `item/completed`) through the state notification queue, `mcp_rpc_contract_test.sa` verifies MCP progress, user input, MCP elicitation, MCP startup status, world-writable warning, skills-changed, fs changed, process output/exited, realtime started/item/audio/transcript/sdp/error/closed event frames including fixed-placeholder rejection for realtime started session ids, and realtime appendAudio/appendText/stop result writer shapes. Existing isolated runtime scripts remain non-unit transport probes; deleted shell tests such as `test_events_contract.sh`, `test_turn_goal_event_thread_id_parity.sh`, `test_mcp_tool_progress_event.sh`, `test_mcp_elicitation_event_shape.sh`, `test_mcp_reload_status_oauth_contract.sh`, `test_realtime_*`, and `test_user_input_event_shape.sh` are historical migration references | Covered by current native default gate for notification frame shapes, realtime append/text/stop response writers, and precise queue coverage for thread/turn/goal/item/fs/process/realtime/MCP/warning families. Live `/events` transport snapshots are non-unit runtime evidence and are not required for the default SA gate |
| MCP JSON-RPC helper surface: OAuth login validation/URL, reload result/startup event, status list, resource read, unsupported resource list/templates, real configured stdio tool execution | `mcp_rpc_contract_test.sa`, `responses_chat_fallback_request_test.sa`, `test_http_contract.sa`; plugin `zig test src/hubproxy_compat.zig` | Covered for current native/plugin gate, including real Code Index `describe-index`, `build-index`, safe-name `build_index`, status pagination, invalid params, and execution failure propagation |
| Request logging, disabled-by-default logs, auth failure previews | `http_dispatch_contract_test.sa` covers the production token-preview helper used by request/auth logs (`none`, `abc...789 (len=15)`, and short-token preview behavior), timestamped `request-*.json` path construction under `HUBPROXY_LOG_DIR`, default no-op behavior for both API request and auth failure log writers when no log dir/request handle is present, the production auth-failure log body builder, the production API request-log body builder, and the shared production log-body file writer. The request-log body native test verifies Deno-shaped `kind:"request-log"`, route `path`, `method`, nested `headers`, Bearer-starts-with-only `authorization` preview, dashed `x-api-key` preview, `content-type`, `bodyBytes`/`body`, `rawBodyBytes`/`rawBody`, and null body/rawBody branches while rejecting auth-failure-only camel-case `xApiKey`. The auth-failure body native test verifies Deno-shaped `kind:"auth_failure"`, route `path`, Bearer-stripped `authorization` preview, camel-case `xApiKey` preview, and `expectedAuth` preview while rejecting the request-log-only dashed `x-api-key` field. The file-writer native test creates the log directory under `/tmp`, writes a generated `request-*.json` file, reads it back through `sa_std_fs_read_file`, and byte-compares it with the generated body. `test_request_log_runtime_isolated.sh` builds a temporary SA binary, starts HubProxy from a temporary cwd with generated `.env`, temp `PORT`, temp `HUBPROXY_LOG_DIR`, and no root `.env`/28080 dependency, then posts an unauthorized `/rpc` request and verifies HTTP-triggered request-log plus auth-failure files contain real request header previews for `authorization`, `x-api-key`, `content-type`, body bytes, and expected auth. The same isolated script now starts a temporary mock chat upstream and posts an authenticated `/v1/chat/completions` request, proving a successful live proxy request also writes a request-log file with route, method, header previews, normalized body bytes, raw body bytes, model, and user content. Deleted shell tests `test_api_request_log.sh`, `test_api_request_log_disabled_by_default.sh`, `test_auth_failure_log.sh` are historical migration references | Covered by current native default gate for redaction, filename, default-disabled behavior, request-log/auth-failure route/body field semantics, request-log body/null branch shape, and actual helper-level file persistence; isolated runtime coverage now verifies live HTTP request-triggered unauthorized `/rpc` request/auth-failure log creation/header extraction plus authenticated live chat proxy success-path request-log creation/body capture |
| Anonymous models while `/rpc` remains auth-protected | `http_dispatch_contract_test.sa` covers the production public-model route helper proving only `GET /v1/models` bypasses auth, plus the production `check_auth` Authorization matcher for strict `Bearer <token>` and exact `x-api-key` semantics; `test_models_runtime_isolated.sh` verifies the same temporary SA process serves unauthenticated `/v1/models` through the mock upstream while unauthenticated `/rpc` returns HTTP 401; `test_http_contract.sa` keeps broad auth/model shape; deleted `test_upstream_auth_headers.sh` is a historical migration reference | Covered for broad auth/model shape, local public-model route gating, local auth matching semantics, and isolated live public-model/protected-RPC route boundary |
| Thin JSON-RPC compatibility responses for experimental features, remote control, login/logout, approvals, command/process control, plugin/install/list/read/share thin responses, marketplace add/remove/upgrade, model list, config requirements, app list, sandbox setup, fuzzy file search/session, and server request resolution | `rpc_emit_contract_test.sa` now covers production result writer helpers for empty `data`/`contents` arrays, empty data pages with `nextCursor:null`, config requirements null fields, plugin install unavailable auth policy, marketplace empty arrays, experimental reasoning feature pages, model capability flags, collaboration mode default/plan entries, hooks list cwd/errors/hooks/warnings shape, account read configured/default email and plan fields plus `requiresOpenaiAuth:false`, account rate-limit configured/default plan and null limit fields, account cancel/logout booleans, approval accept, MCP elicitation accept, permissions defaults, user-input continue answers, command exec `exitCode`/`stdout`/`stderr` results, process spawn `processHandle` results, process-kill-style `ok` bool results, remote-control status params/defaults, external import and ChatGPT token-refresh timestamp payloads with injected ISO values, plugin read/uninstall payloads including default marketplace/null path/summary name and plugin-name echo, plugin share save/checkout/delete/updateTargets payloads including `remotePluginId`, `shareUrl`, `discoverability`, marketplace/plugin echo, null remote id fallback, and principals arrays, marketplace add/remove/upgrade payloads with injected installed root, source/name echo, selected marketplace array, and empty upgrade fallback, `account/sendAddCreditsNudgeEmail` status/email and null-email fallback, plus initialize runtime info (`userAgent`, `codexHome`, `platformFamily`, `platformOs`), `account/login/start` explicit/default type, `environment/add` name/path and null path, `review/start` thread id echo, `mcpServer/oauth/login` authorization URL, `mock/experimentalMethod` node/null echo, `externalAgentConfig/detect` item/path payloads with injected cwd, `windowsSandbox/setupStart` explicit/default mode including Deno's `unelevated` fallback, fuzzy search hit/empty-query payloads, fuzzy session ok/stopped statuses, and serverRequest resolved thread/request echo. `test_rpc_transport_runtime_isolated.sh` adds live `/rpc` HTTP transport evidence for wrapped `initialize`, `config/read`, `config/value/write`, `account/login/start`, and unknown-method envelopes, including string/numeric id preservation. `test_model_list_rpc_runtime_isolated.sh` adds live `/rpc` `model/list` evidence for Deno-style raw upstream `/v1/models` passthrough without a JSON-RPC wrapper. `test_thread_goal_timestamp_runtime_isolated.sh` adds live `/rpc` evidence for goal timestamp set/get envelopes and turn timestamp read/interrupt envelopes. `test_command_process_runtime_isolated.sh` adds live `/rpc` evidence for actual subprocess execution through `command/exec` with stdout/stderr/exit code, Deno's missing-command fallback to `true`, `process/spawn` handle results, and `process/kill` true/false state transitions. It also covers the `remoteControl/status/changed` notification queue path for connected status events, and caught production key-length fixes for `requiresOpenaiAuth` and `rateLimitsByLimitId`. `model_list_contract_test.sa` still keeps ignored runtime-client contracts that compile with `--compile-only --include-ignored`; deleted `test_rpc_misc_parity.sh` is a historical migration reference | Partially covered by current native default gate for writer and notification result shapes, including account/model/collaboration/hooks/rate-limit, command/process, timestamp, plugin read/share/uninstall, marketplace, send-credits, initialize/login/environment/review/oauth/mock, external-detect, sandbox setup, fuzzy search/session, and server-resolved result payloads; isolated runtime now covers deterministic live `/rpc` envelope transport for initialize/config read/write/login/error paths, live `/rpc model/list` upstream passthrough, thread/goal timestamp transport, and actual command/process subprocess behavior. Remaining live evidence is now concentrated in optional broader event-family SSE snapshots and specific proxy/fallback captures that are still intentionally isolated from the default native gate |

## Env / Config / Std Facade

| Deno behavior group | SA coverage | Status |
| --- | --- | --- |
| `.env` load, `SA_PORT=28080`, Deno `PORT` alias, invalid port rejection, null config semantics, config writes | `config_contract_test.sa` covers production `cfg_parse_line` and `cfg_load` behavior for legacy `SA_PORT`, Deno-compatible `PORT`, quote trimming, `CODEX_` key skipping, missing `RESPONSES_BASE_URL` as an empty/null config field, and invalid `PORT`/`SA_PORT` rejection for zero, non-numeric, and >65535 values rather than u16 truncation. `test_config.sa` and `test_server_config.sa` remain historical smoke-style SA files; `rpc_parse_contract_test.sa` covers the production `config/read` result writer for numeric `port`, string `host`/`chatBaseUrl`/`defaultModel`, null `responsesBaseUrl`/`authToken`, empty `layers`/`origins`, and the `config/value/write` result writer for `filePath`, `status:"ok"`, and `version:"1"`. `test_rpc_transport_runtime_isolated.sh` adds live `/rpc` transport evidence for `config/read` from a temporary `.env`, verifying `host`, random `port`, `chatBaseUrl`, `defaultModel`, missing `responsesBaseUrl`, hidden `authToken:null`, empty `layers`/`origins`, and `initialize` cwd acquisition through `codexHome` from the temporary working directory. The same isolated script verifies live `config/value/write` returns `filePath` equal to the temporary cwd with `status:"ok"` and `version:"1"`. Deleted shell tests `test_config_read_nulls.sh`, `test_config_write_cwd.sh` are historical migration references | Covered by current native default gate for parser/config result shapes, Deno `PORT` alias, invalid port rejection, missing Responses base semantics, and config write result shape, plus isolated live `config/read`/`config/value/write` `/rpc` transport and cwd acquisition |
| Root restart entrypoint uses SA binary, preserves Deno on `27787`, and makes `--log-dir` effective for SA logging | Deleted `test_restart_sa_entry.sh` is a historical migration reference | Not part of current default native gate and should remain isolated because it can affect managed ports/processes |
| Default project tasks use SA build/start, with Deno tasks explicit and namespaced | `project_tasks_contract_test.sa` reads the root `deno.json` and verifies default `dev`/`start`/`check` tasks use SA build/restart while Deno commands remain under `deno:*`; deleted `test_project_tasks_sa_default.sh` is a historical migration reference | Covered by current native default gate for project task configuration |
| Deno-like JSON parsing/stringify/fetch/env/fs/process facade used by HubProxy | SCI focused C fixtures plus HubProxy focused tests | Covered enough for current HubProxy; JSON-RPC params string literal is now in SCI std, and remaining old byte-scan logic is cleanup, not current parity blocker |

## Deno Mock Harness

`src/responses_mock_test.ts` mostly tests the Deno-only mock response harness. SA service coverage exists for the externally visible response shapes through the proxy tests above. Do not port the mock harness one-for-one unless SA grows an equivalent mock-server utility.

## Real Upstream Smoke

`src/real_upstream_test.ts` contains three environment-dependent upstream smoke tests for chat, responses, and models. The SA equivalent is not a deterministic unit gate; verify manually or with a focused live smoke when credentials/network are available. Historical live checks have used the project SA entrypoint and the local `.env` to compare `/models`, `/responses`, and `/chat/completions`, but those checks are not part of the current default native gate and were not rerun during the current non-invasive pass. Current default evidence is build/native/plugin tests only; any future live upstream check must avoid modifying `.env`, must not assume ownership of port `28080`, and should record its exact runtime isolation conditions.

## Remaining Root Gaps

- Non-stream chat JSON -> Responses JSON fallback has been moved to SCI std via `sa_deno_chat_json_to_responses`; Responses request -> Chat fallback body construction, including top-level string input and tool-output name inheritance, now runs through SCI std `sa_deno_responses_chat_fallback_request`; chat SSE fallback, including stream tool-call normalization, has been moved to SCI std via `sa_deno_chat_sse_to_responses`; native Responses JSON thinking/message normalization has been moved to SCI std via `sa_deno_responses_json_normalize`; native Responses SSE thinking/MCP normalization now runs directly through `sa_deno_responses_sse_normalize`; JSON-RPC params string literal extraction now runs through SCI std `sa_deno_jsonrpc_params_string_literal`. The old unreferenced HubProxy chat JSON fallback, native SSE thinking, stream tool-call normalization, legacy request/tool fallback byte-scan helpers, old chat fallback tool-history builder, old id-fixed JSON-RPC wrappers, stale thread/start wrapper, trailing import sentinel workaround, fixed-response `_LEN` leftovers, and zero-reference fallback/RPC/event constants have been deleted. Remaining cleanup is to continue deleting or replacing older request/RPC helper code after each path is proven covered by SCI std.
- Token-budget auto-interruption remains listed in `todo.md` as a future optimization, but current Deno code does not enforce proxy token budgets either; Deno only stores `tokenBudget` in goal state and emits token usage notifications. Do not treat budget interruption as a Deno parity blocker unless the TypeScript implementation adds that behavior.
- `turn/start` RPC now avoids non-Deno alias inference and applies Deno-style ASCII trim/lowercase for arbitrary `params.collaborationMode.mode` strings. SA stores the normalized `collaborationModeKind` inline in the turn slot and also keeps `plan`/`goal`/`code` as a byte enum for existing context-mode checks. Current inline storage caps the normalized mode at 64 bytes, which is enough for Codex/Deno collaboration mode names but is still a fixed-buffer implementation limit.
- Codex v2 protocol currently defines `ThreadGoalUpdatedNotification.params.turnId` as nullable, while the Deno HubProxy implementation emits `thread/goal/updated` params as `{ threadId, goal }`. SA intentionally follows Deno here; do not add a `turnId:null` field unless the Deno version changes or the project explicitly switches this event to Codex-protocol-first behavior.
- Current SCI `zig build test` can fan out into broad compiler/plugin work. Use focused runtime build/C fixtures or direct `zig test --test-filter ...` for new std helpers; rerun wider smoke only when the compiler/plugin graph is stable enough for the requested change.
- Shell tests should prefer temporary working directories with generated `.env` files and isolated `SA_TEST_PROXY_PORT` values. Older tests that still rewrite `.env` or bind `28080` must be converted before parallel use.
- `sa/tests/test_http_contract.sa` is a broad smoke contract, not a precise Deno snapshot. Keep assertions on stable externally visible shape; avoid pinning dynamic UUIDs, exact upstream text, or fields that Deno itself does not return.
